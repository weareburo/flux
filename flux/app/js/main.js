require([
	'backbone',
    'views/app'
], function (Backbone, App) {
    var flux = new App();
    // we will need a router at some point so we can go directly to a post //
    // flux.router = new Router();
});
